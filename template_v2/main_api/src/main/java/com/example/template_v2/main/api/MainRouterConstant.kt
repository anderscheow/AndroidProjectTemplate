package com.example.template_v2.main.api

object MainRouterConstant {
    object Activity {
        const val PATH_LOGIN = "/main/LoginActivity"
        const val PATH_SPLASH = "/main/SplashActivity"
        const val PATH_MAIN = "/main/MainActivity"
    }

    object Fragment

    object BottomSheet

    object SendPayload

    object ReturnPayload
}